import { ImageLoader } from 'next/dist/client/image';
declare class CustomImageLoader {
    _loader: undefined | ImageLoader;
    set loader(loader: ImageLoader | undefined);
    get loader(): ImageLoader | undefined;
}
export declare const imageLoader: CustomImageLoader;
export {};
